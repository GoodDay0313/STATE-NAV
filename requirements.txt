# -------- Core Python tooling (keep modern) --------
tqdm>=4.67
pyyaml>=6.0

# -------- Numeric / SciPy stack (Py3.10+ & common ROS2 containers) --------
numpy<2  # lama constraint
scipy==1.11.4
scikit-image>=0.22,<0.24  # lama constraint
scikit-learn>=1.3,<1.6  # lama constraint
matplotlib==3.8.4
PyQt5

# -------- Geometry / utils --------
bezier>=2023.7.28
easydict==1.9.0  # lama constraint
simple-parsing>=0.1.7

# -------- Networking / misc (only keep if you actually use them) --------
aiohttp>=3.10
paramiko>=2.12
ruamel.yaml>=0.18

# -------- CUDA 12.x (CUDA 12.1 runtime) --------
cupy-cuda12x>=13.0

# -------- PyTorch CUDA 12.1 --------
# Install with the PyTorch CUDA 12.1 index URL (see commands below)
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# -------- Vision/ML extras (from lama + main) --------
opencv-python
joblib
albumentations==0.5.2  # lama constraint (older version for compatibility)
hydra-core==1.1.0
pytorch-lightning
tabulate
kornia==0.5.0  # lama constraint (older version for compatibility)
onnx>=1.17
webdataset  # lama constraint (no version specified)
packaging
wldhx.yadisk-direct
gpustat>=1.1.1
